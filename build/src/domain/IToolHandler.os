// Домен: абстракция обработчика инструмента (contracts §5).
// Обработчик принимает arguments (структура/соответствие) и возвращает результат для content.
// В OneScript интерфейс задаётся контрактом: объект с методом Выполнить(Аргументы).
// Возврат: Структура("Текст", Строка, "Ошибка", Булево).
// При успехе: Ошибка = Ложь, Текст = содержимое для result.content[].text.
// При ошибке вызова инструмента: Ошибка = Истина, Текст = описание ошибки (без падения процесса).

// Данный файл задаёт контракт; реализации — в src/handlers (BslSearchHandler и др.).
